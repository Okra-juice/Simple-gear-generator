# Simple Gear Generator（简单齿轮生成器）

一个用 C++（纯 Win32 API）编写的参数化齿轮生成工具。配置参数后可导出可直接 3D 打印的 STL 文件或可导入 CAD 的 STEP 文件，自带**右侧实时 3D 预览**和**中英双语界面**，无需任何第三方库。

---

## 目录

- [快速开始](#快速开始)
- [齿轮类型](#齿轮类型)
- [参数详解](#参数详解)
- [中心孔功能](#中心孔功能)
- [倒角功能](#倒角功能)
- [中心凸台](#中心凸台)
- [多层齿轮（堆叠）](#多层齿轮堆叠)
- [层编辑对话框](#层编辑对话框)
- [3D 实时预览](#3d-实时预览)
- [语言切换](#语言切换)
- [导出格式与单位](#导出格式与单位)
- [从源码编译](#从源码编译)
- [参数校验规则](#参数校验规则)
- [注意事项](#注意事项)

---

## 快速开始

1. **双击运行** `SimpleGearGenerator.exe`（无需安装，单文件即可运行）。
2. 在窗口顶部选择**语言**（中文 / English）和**齿轮类型**。
3. 在左侧面板填写各项参数（单位均为毫米）。
4. 右侧 3D 预览会随参数变化**实时刷新**，拖动旋转、滚轮缩放查看效果。
5. 确认无误后，在底部选择**导出格式**（STL / STEP）和**输出单位**。
6. 点击「生成」按钮，在弹出的对话框中选择保存路径。
7. STL 文件可用切片软件（Cura、PrusaSlicer 等）直接 3D 打印；STEP 文件可导入 SolidWorks、FreeCAD 等 CAD 软件进一步编辑。

---

## 齿轮类型

本工具支持 **5 种**齿轮类型，在界面顶部的下拉框中选择：

### 1. 普通齿轮（Spur Gear）

标准渐开线直齿轮，最常用的齿轮形式。

- **齿形**：标准渐开线齿廓，压力角 20°
- **模数计算**：`模数 = 外径 ÷ (齿数 + 2)`
- **齿根**：齿根圆直径 = `模数 × (齿数 - 2.5)`（1.25 倍齿根间隙）
- **齿廓采样**：每齿由半齿廓旋转 + 镜像拼接而成，包含 4 个齿顶点 + 12 个渐开线侧点 + 3 个齿根点

**可用参数**：外径、齿数、厚度、中心凸台、中心孔、倒角

### 2. 冠状轮 / 桶形齿轮（Crowned Gear）

鼓形齿轮，齿沿轴向呈桶形分布（中间粗、两端细），用于补偿轴间不对中。

- **齿形**：基于标准渐开线齿廓，沿厚度方向附加径向膨胀
- **桶形量**：中间截面比两端径向膨胀的量
- **限制**：桶形量须小于 `1.1 × 模数`

**可用参数**：外径、齿数、厚度、桶形量、中心凸台、中心孔

### 3. 垂直轮 / 冠齿轮（Crown Wheel）

圆盘薄片 + 一圈沿轴向立起的梯形齿，形似皇冠，可与普通齿轮呈 90° 轴线啮合。

- **结构**：底部为圆盘薄片，齿沿圆盘外缘向上立起
- **薄片厚度**：圆盘部分厚度
- **齿高**：立起齿沿轴向的高度

**可用参数**：外径、齿数、薄片厚度、齿高、中心凸台、中心孔

### 4. 内齿轮（Internal Gear）

环形齿轮，齿分布在圆环内侧，可与普通齿轮外啮合组成行星齿轮机构。

- **生成方法**：先构建标准外齿轮廓，再以节圆为对称轴将齿廓反向（`ρ → 2r - ρ`），使齿尖指向内部
- **模数计算**：`模数 = 外径 ÷ (齿数 + 6.5)`（与外齿轮公式不同）
- **结构**：外边界为光滑圆环，内边界为内齿廓
- **外径含义**：此处的「外径」指圆环外径，非齿顶圆直径

**可用参数**：外径（圆环外径）、齿数、厚度、倒角
> 注意：内齿轮不支持中心凸台和中心孔（中心已经是空的）。

### 5. 多层齿轮 / 堆叠齿轮（Stacked Gear）

将多个不同类型/参数的齿轮沿 Z 轴堆叠成一个整体零件，例如行星齿轮系或复合齿轮。

- **层数**：无上限（至少 1 层）
- **每层独立**：每层可分别设置齿轮类型、外径、齿数、厚度等
- **层高累加**：各层沿 Z 轴依次堆叠，高度 = 上一层高度 + 本层高度
- **支持的子类型**：普通齿轮、冠状轮、垂直轮、内齿轮（不支持嵌套堆叠）

**操作方式**：选择此类型后，左侧面板切换为层管理界面，通过列表 + 按钮管理各层。


## 参数详解

### 基础参数

| 参数 | 单位 | 适用类型 | 说明 |
|------|------|----------|------|
| 外径 | mm | 全部 | 普通齿轮为齿顶圆直径；内齿轮为圆环外径；垂直轮为齿环外径 |
| 齿数 | — | 全部 | 齿轮齿数，最小值 5 |
| 厚度 | mm | 普通 / 冠状 / 内齿 | 齿轮主体厚度 |
| 桶形量 | mm | 冠状轮 | 中间截面比两端径向膨胀的量，须 < 1.1×模数 |
| 薄片厚度 | mm | 垂直轮 | 圆盘薄片部分的厚度 |
| 齿高 | mm | 垂直轮 | 沿轴向立起齿的高度 |

### 模数与齿形

- **压力角**：固定 20°（标准值），不可调整
- **渐开线求解**：使用对分法（bisection，80 次迭代）求解渐开线函数 `inv(α) = tan(α) - α`
- **基圆半径**：`rb = r × cos(20°)`
- **齿根间隙**：1.25 倍模数（标准值）
- **模数显示**：生成成功后，日志中会显示自动计算的模数（如 `模数=2.000 mm`）

---

## 中心孔功能

在齿轮中心创建通孔，用于安装在轴上。勾选「添加中心孔」后启用。

### 孔直径

- 圆形孔：即圆孔直径
- 方形口：即正方形边长
- 多边形孔：即外接圆直径
- 星形孔：即外接圆直径（内径为外径的 50%）
- D 型口：由「长」和「宽」两个参数定义（见下文）

### 孔形状

不勾选「自定义孔形状」时为默认的**圆形孔**。勾选后可选择以下 5 种形状：

| 形状 | 说明 | 额外参数 |
|------|------|----------|
| 圆形 | 标准圆孔，128 段逼近 | 孔直径 |
| D 型口 | 一侧平切的圆形孔（D 形截面） | 长、宽、可锁定长宽比 |
| 方形口 | 正方形孔，边长 = 孔直径 | 孔直径 |
| 多边形孔 | 正多边形孔（3 边以上） | 外接圆直径、边数（≥3，默认 6） |
| 星形孔 | 星形截面孔 | 外接圆直径、角数（≥3，默认 5） |

### D 型口参数

- **长**：D 形截面的全长
- **宽**：D 形截面的宽度
- **锁定长宽比**：勾选后修改长或宽时自动保持比例（宽/长 ≈ 0.8）
- **约束**：需满足 `长的一半 < 宽 < 长`

### 孔尺寸限制

孔的最大外接直径必须小于限制直径，否则报错：
- 有凸台时：限制直径 = 凸台直径
- 无凸台时：限制直径 = 齿根圆直径
- 垂直轮：限制直径 = 外径 × 0.62

---

## 倒角功能

在齿轮边缘加工斜角，减少应力集中和毛刺。仅适用于**普通齿轮**和**内齿轮**。

勾选「倒角」复选框后启用，可设置倒角大小和位置。

### 倒角位置

| 选项 | 说明 |
|------|------|
| 无 | 不做倒角（即使勾选了倒角，位置选「无」也等同于不倒角） |
| 顶边 | 在齿轮顶面边缘倒角 |
| 底边 | 在齿轮底面边缘倒角 |
| 顶底边 | 顶面和底面同时倒角 |
| 齿尖 | 在每个齿的齿尖处倒角（缩小齿顶圆） |

> 注意：内齿轮不支持「齿尖」倒角，仅支持顶边 / 底边 / 顶底边。

### 倒角大小

- 倒角的实际高度会被自动限制：`chamfH = min(倒角大小, 厚度 × 0.4)`，防止过度倒角
- 倒角大小须 > 0

### 倒角原理

- **顶边/底边**：将轮廓按比例 `((齿顶半径 - 倒角大小) / 齿顶半径)` 缩小，在完整轮廓与缩小轮廓之间建立斜面壁
- **齿尖**：将整个外轮廓向内缩进倒角大小的量（齿顶圆半径减小）
- **内齿轮**：外圆环和内齿廓同时缩放（外环向内收，内齿廓向中心移动）

---

## 中心凸台

在齿轮顶面中央添加凸起的圆柱形轮毂，用于安装或定位。勾选「添加中心凸起（凸台）」后启用。

| 参数 | 说明 |
|------|------|
| 凸起厚度 | 凸台高度（位于齿轮顶面一侧，向上凸起） |
| 凸起直径 | 凸台圆柱直径 |

### 凸台限制

- 凸起直径须 < 限制直径：
  - 普通齿轮：限制直径 = 齿根圆直径
  - 冠状轮：限制直径 = 齿根圆直径 - 2 × 桶形量
  - 垂直轮：限制直径 = 外径 × 0.62
- 凸起厚度须 > 0
- 如同时有中心孔，凸台为中空环形（孔穿过凸台）

> 注意：内齿轮不支持中心凸台。

---

## 多层齿轮（堆叠）

选择齿轮类型为「多层齿轮（堆叠）」时，左侧面板切换为层管理界面。

### 层管理控件

| 控件 | 说明 |
|------|------|
| 层数 | 显示当前总层数，可直接编辑修改 |
| 添加层 | 在末尾添加一层默认参数的普通齿轮（外径 40mm，20 齿，5mm 厚） |
| 删除层 | 删除列表中当前选中的层 |
| 层列表 | 5 列表格：序号 / 类型 / 外径 / 齿数 / 高度 |

### 默认层配置

初始有 2 层：
- 第 1 层：普通齿轮，外径 40mm，20 齿，厚 5mm，凸台 5mm×20mm
- 第 2 层：普通齿轮，外径 30mm，15 齿，厚 4mm

### 堆叠高度计算

每层的高度 = 该层齿轮厚度 + 该层凸台厚度（如有），各层沿 Z 轴依次累加。

### 支持的子类型

每层可设置为以下 4 种之一：普通齿轮、冠状轮、垂直轮、内齿轮（不支持在堆叠内再嵌套堆叠）。

---

## 层编辑对话框

在层列表中**双击某一行**可打开层编辑对话框，对该层的参数进行详细配置。

### 对话框控件

对话框包含以下控件，会根据所选齿轮类型动态显示/隐藏：

| 控件 | 适用类型 | 说明 |
|------|----------|------|
| 齿轮类型 | 全部 | 下拉选择（普通 / 冠状 / 垂直 / 内齿） |
| 外径 | 全部 | 齿轮外径 |
| 齿数 | 全部 | 齿数（≥5） |
| 厚度 | 普通 / 冠状 / 内齿 | 主体厚度 |
| 薄片厚度 | 垂直轮 | 圆盘厚度 |
| 齿高 | 垂直轮 | 齿沿轴向高度 |
| 桶形量 | 冠状轮 | 径向膨胀量 |
| 添加中心凸台 | 普通 / 冠状 / 垂直 | 复选框，勾选后显示凸台厚度 + 凸台直径 |
| 添加中心孔 | 普通 / 冠状 / 垂直 | 复选框 + 孔直径 |
| 孔形状 | 普通 / 冠状 / 垂直 | 下拉选择（圆 / D 型 / 方形 / 多边形 / 星形） |
| 倒角 | 普通 / 内齿 | 复选框，勾选后显示倒角大小 + 倒角位置 |
| 确定 / 取消 | — | 确认或放弃修改 |

### 动态显示规则

- 选择**内齿轮**：隐藏凸台和中心孔控件，显示倒角控件
- 选择**垂直轮**：隐藏厚度，显示薄片厚度 + 齿高
- 选择**冠状轮**：显示桶形量
- **倒角**控件仅在普通齿轮和内齿轮类型下显示

---

## 3D 实时预览

右侧 3D 预览面板使用纯软件渲染器（无需 GPU），提供以下交互：

### 操作方式

| 操作 | 效果 |
|------|------|
| 鼠标左键拖动 | 旋转视角（偏航 yaw + 俯仰 pitch），俯仰角限制在 ±1.45 弧度 |
| 鼠标滚轮 | 缩放（每格 ×1.15 倍，范围 0.1～20.0） |

### 渲染特性

- **投影**：正交投影，以网格质心为中心
- **光照**：兰伯特光照模型，光源方向 (0.45, -0.55, 0.7)
- **着色**：33 级灰度，基色 RGB(182, 188, 198)
- **背面剔除**：不渲染不可见面
- **深度排序**：画家算法（按三角形平均 Y 值排序）
- **双缓冲**：离屏 DC + 位图，避免闪烁
- **背景色**：浅灰 RGB(246, 248, 251)

### 预览更新

- 任何参数修改（文本输入 `EN_CHANGE` 事件）都会触发网格重建和重绘
- 切换齿轮类型、勾选/取消凸台/孔/形状等复选框也会触发重新布局和重建

---

## 语言切换

界面顶部有语言下拉框，支持两种语言即时切换：

| 选项 | 说明 |
|------|------|
| 中文 | 所有界面文字变为中文（默认） |
| English | 所有界面文字变为英文 |

切换语言时会立即更新所有控件文本、下拉框选项、列表列标题等，不影响已输入的参数值。

> 注意：日志窗口的输出始终为英文（不影响使用）。

---

## 导出格式与单位

### 导出格式

| 格式 | 默认文件名 | 文件过滤 | 适用场景 |
|------|-----------|----------|----------|
| STL（二进制） | gear.stl | *.stl | 3D 打印（切片软件） |
| STEP（AP214） | gear.step | *.step | CAD 导入编辑（SolidWorks / FreeCAD 等） |

- **STL 格式**：标准二进制 STL，80 字节文件头 + 三角面数 + 每个三角形（法向量 + 3 顶点 + 属性）
- **STEP 格式**：完整 STEP AP214（ISO 10303-21），每个三角面转换为 ADVANCED_FACE → EDGE_LOOP → VERTEX，组装为 CLOSED_SHELL → MANIFOLD_SOLID_BREP，单位为毫米

### 输出单位

| 选项 | 缩放因子 | 说明 |
|------|----------|------|
| 毫米 | 1.0 | 默认，适合大多数 3D 打印 |
| 厘米 | 0.1 | 导出坐标 ×0.1 |
| 英寸 | 1/25.4 | 导出坐标 ÷25.4 |
| 米 | 0.001 | 导出坐标 ×0.001 |

界面输入始终为毫米，仅导出时按所选单位换算坐标值。

### 生成流程（6 步）

点击「生成」按钮后，日志窗口会显示 6 步进度：

1. **[1/6] 读取参数**：从界面读取所有参数（堆叠模式从层列表读取）
2. **[2/6] 校验参数**：检查所有参数合法性，失败则中止并报错
3. **[3/6] 构建网格**：计算渐开线齿廓并构建 3D 三角网格
4. **[4/6] 准备导出**：确定格式、单位、缩放
5. **[5/6] 选择输出文件**：弹出保存对话框选择路径
6. **[6/6] 写入文件**：写入 STL 或 STEP，显示成功/失败信息

---

## 从源码编译

源码为单文件 `SimpleGearGenerator.cpp` + 资源文件 `app.rc` + 图标 `app.ico`，无第三方依赖，仅需 Windows SDK。

### Visual Studio / MSVC

```bash
rc /nologo /fo app.res app.rc
cl /EHsc /O2 /utf-8 SimpleGearGenerator.cpp app.res /link /SUBSYSTEM:WINDOWS /OUT:SimpleGearGenerator.exe
```

- `/EHsc`：C++ 异常处理
- `/O2`：速度优化
- `/utf-8`：源码以 UTF-8 编码
- `/SUBSYSTEM:WINDOWS`：窗口子系统（不弹出控制台）

### MinGW (g++)

```bash
windres app.rc -o app.res
g++ -O2 SimpleGearGenerator.cpp app.res -o SimpleGearGenerator.exe -mwindows -lcomctl32 -lcomdlg32 -luser32 -lgdi32
```

- `-mwindows`：Windows 窗口程序
- `-lcomctl32`：通用控件库（ComboBox、ListView 等）
- `-lcomdlg32`：通用对话框库（文件保存对话框）
- `-luser32` / `-lgdi32`：用户界面 + 图形设备接口

---

## 参数校验规则

生成前会自动校验所有参数，不符合则报错中止：

| 校验项 | 规则 | 错误提示 |
|--------|------|----------|
| 外径 | > 0 | 外径必须大于 0 |
| 齿数 | ≥ 5 | 齿数至少为 5 |
| 厚度 | > 0 | 厚度必须大于 0 |
| 桶形量 | > 0 且 < 1.1×模数 | 桶形量过大：须小于 1.1×模数 |
| 薄片厚度 | > 0 | 薄片厚度必须大于 0 |
| 齿高 | > 0 | 齿高必须大于 0 |
| 凸起厚度 | > 0 | 凸起厚度必须大于 0 |
| 凸起直径 | > 0 且 < 限制直径 | 凸起直径不能大于等于最大允许直径 |
| 孔直径 | > 0 | 孔直径必须大于 0 |
| D 型孔长宽 | > 0 且 半长 < 宽 < 长 | D 型孔的宽需满足：长的一半 < 宽 < 长 |
| 多边形边数 | ≥ 3 | 多边形边数至少为 3 |
| 星形角数 | ≥ 3 | 星形角数至少为 3 |
| 孔尺寸 | 最大外接直径 < 限制直径 | 孔尺寸过大 |
| 倒角大小 | > 0（勾选倒角时） | 倒角大小必须大于 0 |
| 堆叠层数 | ≥ 1 | 请至少添加一个齿轮层 |

---

## 注意事项

1. **齿根过渡**：采用直线简化（非精确齿根圆角），对 3D 打印精度影响极小。
2. **凸台方向**：凸台位于齿轮顶面一侧。如需两侧对称凸台，可导出后在切片软件中镜像拼接，或修改源码中凸台生成逻辑。
3. **STL 单位**：STL 文件本身不记录单位，切片/CAD 软件会按各自默认单位解释坐标。若你的软件默认单位不是毫米，请在「输出单位」中选择对应单位（例如 Blender 选「米」、以英寸为单位的 CAD 选「英寸」）。
4. **内齿轮不支持**：中心凸台、中心孔（中心已是空的）。
5. **内齿轮倒角**：不支持「齿尖」位置，仅支持顶边 / 底边 / 顶底边。
6. **堆叠限制**：不支持在堆叠层中嵌套堆叠。
7. **实时预览**：预览为软件渲染（CPU），复杂齿轮（齿数多 + 堆叠层数多）可能略有卡顿，属正常现象。
8. **日志语言**：日志窗口输出始终为英文，即使界面切换为中文，不影响参数设置和文件生成。
